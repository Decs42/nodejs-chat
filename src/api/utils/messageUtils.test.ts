import { describe, it } from 'node:test';

/**
 * Message Utils Tests
 * Primary Goal: tests fetching of all chats
 */

describe('userUtils', () => {
    it('fetch all chats', () => {
        
    })
})

/**
 * Message Utils Tests
 * Primary Goal: tests creating chat message
 */

describe('userUtils', () => {
    it('create chat message', () => {
        
    })
})

/**
 * Message Utils Tests
 * Primary Goal: tests deletion of chat message
 */

describe('userUtils', () => {
    it('delete chat message', () => {
        
    })
})