import { describe, it } from 'node:test';

/**
 * User Utils Tests
 * Primary Goal: tests fetching of all users
 */

describe('userUtils', () => {
    it('fetch all users', () => {
        
    })
})

/**
 * User Utils Tests
 * Primary Goal: tests creation of many users
 */

describe('userUtils', () => {
    it('create many users', () => {
        
    })
})