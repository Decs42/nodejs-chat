import { describe, it } from 'node:test';

/**
 * WebSocket Utils Tests
 * Primary Goal: tests handling of connection error
 */

describe('websocketUtils', () => {
    it('handle connection error', () => {
        
    })
})

/**
 * WebSocket Utils Tests
 * Primary Goal: authenticate user test
 */

describe('websocketUtils', () => {
    it('authenticate user', () => {
        
    })
})

/**
 * WebSocket Utils Tests
 * Primary Goal: tests connection action handler
 */

describe('websocketUtils', () => {
    it('action handler', () => {
        
    })
})